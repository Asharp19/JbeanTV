# This ensures Prisma works properly on Vercel
# Disable husky hooks which may cause issues in Vercel builds
ignore-scripts=false
# Disable legacy peer deps check to avoid unnecessary warnings
legacy-peer-deps=true
# Ensure node-fetch is used for Prisma
public-hoist-pattern[]=*node-fetch* 